# Database
DATABASE_URL="postgresql://user:password@localhost:5432/jamsawli"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-here"

# OAuth Providers (optional if using phone OTP only)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Razorpay
RAZORPAY_KEY_ID=""
RAZORPAY_KEY_SECRET=""
NEXT_PUBLIC_RAZORPAY_KEY_ID=""

# App / multi-tenant
NEXT_PUBLIC_APP_NAME="Jamsawli Hanuman Lok"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_DEFAULT_TENANT="jamsawli-hanuman"

# Phone OTP (optional — without these, OTP logs to server console in dev)
MSG91_AUTH_KEY=""
MSG91_TEMPLATE_ID=""
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_FROM=""
# OTP_DEV_EXPOSE="true"  # force return OTP in API even in production (never on real prod)

# WhatsApp Business API
WHATSAPP_TOKEN=""
WHATSAPP_PHONE_NUMBER_ID=""
WHATSAPP_VERIFY_TOKEN=""

# OpenAI (for Chatbot)
OPENAI_API_KEY=""

# Email (Resend) — weekly trustee reports + transactional mail
RESEND_API_KEY=""
RESEND_FROM="MandirOS <onboarding@resend.dev>"
# Optional default recipient when temple has no emails configured
WEEKLY_REPORT_EMAIL=""

# Slack — optional platform-wide fallback webhook for weekly reports
SLACK_WEBHOOK_URL=""

# Cron — protect /api/cron/weekly-reports (Vercel cron Monday 03:00 UTC)
# Set the same value in Vercel env; Authorization: Bearer <secret> or x-cron-secret
CRON_SECRET=""
